[project]
name = "bup"
version = "0.1.0"
description = "GridWise LLM — smart campus microgrid optimization API (BUP CSE Fest 2026)"
readme = "README.md"
requires-python = ">=3.12.3"
dependencies = [
    "fastapi>=0.111",
    "openai>=1.40",
    "pulp>=2.8",
    "pydantic>=2.7",
    "python-dotenv>=1.0",
    "requests>=2.31",
    "uvicorn[standard]>=0.30",
]

[tool.uv]
package = false
